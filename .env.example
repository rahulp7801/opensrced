# Optional: When set, the dashboard proxies API calls to a running ContribAI web-server.
# If unset, the dashboard serves seeded observatory data from its own /api routes.
# CONTRIBAI_API_URL=http://127.0.0.1:8787
# CONTRIBAI_API_KEY=
