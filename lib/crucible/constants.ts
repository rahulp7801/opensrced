// Constants shared between the crucible connect-start route and the
// install-callback route. Kept outside app/ so Next.js doesn't complain
// about non-route exports from a route file.

export const STATE_COOKIE = "crucible-state";
